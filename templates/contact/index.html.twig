{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}


<div class="container dick text-white min-vh-100 d-flex justify-content-center align-items-center">
    
    <div class="ensemble">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-4">
            {{ message }}
        </div>
    {% endfor %}

    <h1>Contactez-nous !</h1>
    {{form(formulaire)}}
    </div>

</div>

{% endblock %}
