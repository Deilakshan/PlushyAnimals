{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="container bob text-white min-vh-100 d-flex justify-content-center align-items-center">
		<div class="ensemble">

        <h1>Edit User</h1>

        {{ include('profile/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_profile_show') }}" class="btn btn-warning">back to profile</a>

        </div>
    </div>

    
{% endblock %}
